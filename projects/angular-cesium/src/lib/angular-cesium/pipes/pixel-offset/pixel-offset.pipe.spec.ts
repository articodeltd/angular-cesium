/* eslint-disable @typescript-eslint/no-unused-vars */
import { PixelOffsetPipe } from './pixel-offset.pipe';

describe('PixelOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new PixelOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
